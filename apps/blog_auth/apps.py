from django.apps import AppConfig

class BlogAuthConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.blog_auth'  # Asegúrate de que el nombre corresponde a la ruta de la app.

